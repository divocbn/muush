import { sleep } from "@/lib/utils/sleep";
import { DoorClosed, DoorOpen, PersonStanding } from "lucide-react";
import Image from "next/image";

export default async function CamPlayerComponent() {
  return (
    <div className="min-w-[60rem] h-full w-full bg-white/10 rounded-l relative" />
  );
}
